{
  "name": "undercity",
  "version": "1.0.0",
  "description": "Visual flow IDE — MUD-style logic graph builder with Bootstrap code generation",
  "type": "module",
  "main": "server.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
